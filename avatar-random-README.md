# 头像随机选择功能使用说明

## 功能介绍

该功能可以让主页头像随机从 `assets/img/myself/` 目录下的图片中选择一张进行显示。不包括"原图"文件夹下的图片。

## 如何使用

### 1. 添加新图片

直接将新图片添加到 `assets/img/myself/` 目录下即可。

### 2. 自动更新图片列表

在项目根目录下运行以下命令，自动更新 `index.html` 中的图片列表：

```bash
npm run build
```

该命令会扫描 `assets/img/myself/` 目录下的所有图片文件（不包括"原图"文件夹），并自动更新 `index.html` 中的 `avatarImages` 数组。

### 3. 提交代码

运行 `npm run build` 命令后，`index.html` 文件会被自动修改，包含最新的图片列表。此时可以提交代码到 GitHub 仓库。

### 4. 刷新页面

代码提交后，等网页重构完成，刷新浏览器页面，新添加的图片就会被包含在随机选择的范围内。

## 支持的图片格式

- .jpg
- .jpeg
- .png
- .gif
- .webp

## 注意事项

1. 请确保添加的图片格式正确，否则可能无法被识别。
2. 请不要在"原图"文件夹下添加需要随机显示的图片，该文件夹下的图片不会被包含在随机选择的范围内。
3. 如果 `npm run build` 命令执行失败，可以检查控制台输出的错误信息，了解具体原因。
4. 如果图片无法显示，可以检查浏览器控制台的网络请求和错误信息，了解具体原因。